[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# No healthcheckPath — Railway healthchecks were failing on HTTP→HTTPS 301 redirects.
# App start is handled by Dockerfile ENTRYPOINT (/entrypoint.sh).
